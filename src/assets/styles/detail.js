import {StyleSheet} from 'react-native';

const detailStyles = StyleSheet.create({
  title: {
    fontSize: 22,
    fontWeight: 'bold',
  },
  subtitle: {
    fontSize: 16,
  },
});

export default detailStyles;
